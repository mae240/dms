name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Stage 1: fast quality gate ───────────────────────────────
  lint:
    name: Ruff (lint + format)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          version: "0.8.4"
          args: check .
      - uses: astral-sh/ruff-action@v3
        with:
          version: "0.8.4"
          args: format --check .

  # ── Stage 2: verification (runs once lint passes) ────────────
  test:
    name: Backend + Worker (pytest, migrations)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare environment
        run: cp .env.example .env
      - name: Migration drift check (upgrade head, then alembic check)
        run: docker compose run --rm backend sh -c "alembic upgrade head && alembic check"
      - name: Backend tests
        run: docker compose run --rm -w /app/backend backend pytest
      - name: Worker tests
        run: docker compose run --rm --no-deps -w /app/worker worker pytest

  frontend:
    name: Frontend (typecheck, build, tests)
    needs: lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Lint (eslint)
        run: npm run lint
      - name: Typecheck + build
        run: npm run build
      - name: Unit tests (vitest)
        run: npm test
      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

  audit:
    name: Dependency audit (pip-audit)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install project dependencies
        run: |
          python -m pip install --upgrade pip setuptools
          pip install uv pip-audit
          uv pip install --system -e packages/dms_core -e "backend[dev]" -e worker
      - name: Audit installed dependencies
        run: pip-audit

  # ── Stage 3: aggregate gate (single required status check) ───
  ci:
    name: CI passed
    needs: [test, frontend, audit]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint, tests, migrations, frontend and dependency audit all passed."
